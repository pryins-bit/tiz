name: tmp-sinilink-smoke
on:
  push:
    branches:
      - tmp/sinilink-apk-build-20260821
  pull_request:
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo public-actions-ok
